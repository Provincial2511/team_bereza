from __future__ import annotations

from transformers import AutoModelForCausalLM, AutoTokenizer

import torch


class LocalGenerator:
    """
    Generate clinical recommendations using a local HuggingFace transformer model.

    This class:
    - Loads a causal language model locally (no external APIs).
    - Constructs prompts explicitly from retrieved guideline sections and patient data.
    - Generates text continuations using the model.

    Prompt construction is explicit to maintain full control over the input format
    and ensure reproducibility. The prompt format combines:
    1. Retrieved clinical guideline sections (context)
    2. Patient information
    3. A clear instruction for the model
    """

    def __init__(self, model_name: str, device: str = "cpu") -> None:
        """
        Initialize the local generator with a HuggingFace model.

        Args:
            model_name: Name or path of the HuggingFace causal language model.
            device: Device identifier, e.g. "cpu" or "cuda".
        """
        self.model_name = model_name
        self.device = torch.device(device)

        self.tokenizer = AutoTokenizer.from_pretrained(model_name)
        self.model = AutoModelForCausalLM.from_pretrained(model_name).to(self.device)

        # Set padding token if not present
        if self.tokenizer.pad_token is None:
            self.tokenizer.pad_token = self.tokenizer.eos_token

    def generate(
        self,
        patient_text: str,
        retrieved_sections: list[str],
        mode: str,
        max_new_tokens: int = 512,
    ) -> str:
        """
        Generate a clinical recommendation based on guideline sections and patient data.

        Args:
            patient_text: Text containing patient information.
            retrieved_sections: List of relevant guideline section texts.
            mode: Generation mode; must be "doctor" or "patient".
            max_new_tokens: Maximum number of tokens to generate.

        Returns:
            Generated recommendation text (prompt stripped).
        """
        if mode == "doctor":
            system_prompt = """
            Ты — врач-онколог с 10-летним опытом работы в ведущем онкологическом центре.

            Твоя задача — провести объективную, доказательную оценку выбранной тактики лечения пациента строго ТОЛЬКО на основании предоставленных ниже фрагментов клинических рекомендаций (раздел «Клинические рекомендации»).

            ❗ Запрещено:
            — использовать внешние знания, не содержащиеся в предоставленных ниже фрагментах;
            — ссылаться на источники, которых нет в тексте клинических рекомендаций ниже;
            — дополнять рекомендации личным опытом или предположениями;
            — выдумывать статистику, выживаемость или уровни доказательности;
            — пересказывать историю пациента дословно или многократно повторять уже сказанное.

            Если в предоставленных фрагментах отсутствуют данные, необходимые для оценки, укажи:
            «Недостаточно данных в предоставленных клинических рекомендациях».

            🔹 Инструкция:
            Сопоставь историю лечения пациента (из блока "Данные пациента") с предоставленными ниже клиническими рекомендациями из блока "Клинические рекомендации".
            Сконцентрируйся на следующем:
            — какие этапы лечения соответствуют рекомендациям;
            — какие отклоняются;
            — возможные альтернативные варианты, если они прямо предусмотрены предоставленными рекомендациями;
            — молекулярно-генетические данные (EGFR, KRAS, NRAS, BRAF, BRCA1/2 и др.), если указаны, и соответствие назначенных препаратов рекомендациям.

            Подавай текст:
            — без таблиц, списков, ключ-значение, Python или JSON;
            — минимум пересказа истории болезни;
            — профессиональным, корректным тоном (коллега — коллеге);
            — избегай повторов;
            — используй термин «прогрессирование»;
            — не упоминай организации или документы, если они не указаны в тексте рекомендаций.

            В конце ответа ОБЯЗАТЕЛЬНО добавь фразу:

            Не является медицинской рекомендацией! Материал создан нейросетью. Используйте в ознакомительных целях.
            """

        elif mode == "patient":
            system_prompt = """
            Ты — врач-онколог с 10-летним опытом работы в ведущем онкологическом центре. Твой собеседник — пациент, проходящий лечение.

            Твоя задача — объяснить, что происходит, почему выбрана именно эта тактика, и насколько она соответствует современным официальным клиническим рекомендациям, предоставленным ниже в разделе «Клинические рекомендации», — доступным и поддерживающим языком. Пациент должен начать доверять.

            ❗ Важно:
            — Используй ТОЛЬКО те клинические рекомендации, которые приведены ниже.
            — Не используй внешние знания и не добавляй сведения, которых нет в предоставленных фрагментах.
            — Не упоминай документы или организации, если они прямо не указаны в тексте рекомендаций.
            — Не выдумывай статистику и препараты.
            — Не фантазируй.
            — Не пересказывай многократно историю болезни, сосредоточься на объяснении и поддержке.

            🔹 Твои действия:

            1. Кратко и понятно обозначь ситуацию:
            — диагноз и основные особенности, простыми словами;
            — что уже сделано (операция, химиотерапия), когда и зачем;
            — текущая стадия и признаки прогрессирования или стабильности.

            2. Объясни, почему выбрана именно эта тактика, ссылаясь только на предоставленные клинические рекомендации:
            — Используй доступные формулировки, избегай уровней доказательности или классов рекомендаций;
            — Объясняй человеческим языком, фокусируясь на том, что важно знать пациенту.

            3. Если что-то не по стандарту — мягко, без обвинений:
            — «Это не стандартный подход, но он мог быть выбран с учётом индивидуальных особенностей».
            — Не называй решение ошибкой, если это прямо не следует из рекомендаций.

            4. Дай ориентир на ближайшее время (только если это прямо следует из предоставленных рекомендаций):
            — следующий этап лечения;
            — необходимость контрольных обследований;
            → наблюдение за побочными эффектами.

            5. Если данных не хватает — честно сообщи:
            — «Чтобы точнее оценить ситуацию, нам важно уточнить…»
            — «Без этих данных сложно определить наиболее подходящую тактику на основании клинических рекомендаций».

            🔹 Правила подачи:

            — Говори как человек, а не как протокол;
            — Избегай паники и катастрофизма;
            — Не используй латинские термины без пояснения;
            — Не упоминай «не соответствует рекомендациям» без оговорки;
            — Не добавляй препаратов или схем, если их нет в тексте рекомендаций ниже;
            — Подчёркивай командный подход и сохранение качества жизни;
            — Минимизируй повторение одних и тех же фраз.

            В конце ответа ОБЯЗАТЕЛЬНО добавь фразу:

            Не является медицинской рекомендацией! Материал создан нейросетью. Используйте в ознакомительных целях.
            """
        else:
            raise ValueError("mode must be 'doctor' or 'patient'")

        retrieved_context = "\n\n".join(retrieved_sections)

        prompt = f"""
        {system_prompt}

        === Клинические рекомендации ===
        {retrieved_context}

        === Данные пациента ===
        {patient_text}
        """

        inputs = self.tokenizer(
            prompt,
            return_tensors="pt",
            truncation=True,
        ).to(self.device)

        with torch.no_grad():
            output_ids = self.model.generate(
                **inputs,
                max_new_tokens=max_new_tokens,
                do_sample=False,
                pad_token_id=self.tokenizer.pad_token_id,
            )

        # Decode full output
        full_output = self.tokenizer.decode(output_ids[0], skip_special_tokens=True)

        # Strip the prompt to return only the generated continuation
        if full_output.startswith(prompt):
            generated = full_output[len(prompt) :].strip()
        else:
            # Fallback: decode only new tokens
            input_length = inputs["input_ids"].shape[1]
            new_tokens = output_ids[0][input_length:]
            generated = self.tokenizer.decode(new_tokens, skip_special_tokens=True)

        return generated

