from __future__ import annotations

import torch
from transformers import AutoModelForCausalLM, AutoTokenizer


class LocalGenerator:
    """
    Generate clinical recommendations using a local HuggingFace chat model.

    This class:
    - Loads a causal language model locally (no external APIs).
    - Formats prompts via ``tokenizer.apply_chat_template`` for proper
      instruction-following with chat models such as Qwen2-7B-Instruct.
    - Supports two generation modes: ``"doctor"`` and ``"patient"``.
    - Returns only the newly generated tokens (no prompt stripping required).
    """

    _DISCLAIMER = (
        "\n\nНе является медицинской рекомендацией! "
        "Материал создан нейросетью. Используйте в ознакомительных целях."
    )

    _SYSTEM_PROMPTS: dict[str, str] = {
        "doctor": (
            "Ты — врач-онколог с 10-летним опытом работы в ведущем онкологическом центре.\n\n"
            "Твоя задача — провести объективную, доказательную оценку выбранной тактики лечения "
            "пациента строго ТОЛЬКО на основании предоставленных ниже фрагментов клинических "
            "рекомендаций (раздел «Клинические рекомендации»).\n\n"
            "❗ Запрещено:\n"
            "— использовать внешние знания, не содержащиеся в предоставленных ниже фрагментах;\n"
            "— ссылаться на источники, которых нет в тексте клинических рекомендаций ниже;\n"
            "— дополнять рекомендации личным опытом или предположениями;\n"
            "— выдумывать статистику, выживаемость или уровни доказательности;\n"
            "— пересказывать историю пациента дословно или многократно повторять уже сказанное.\n\n"
            "Если в предоставленных фрагментах отсутствуют данные, необходимые для оценки, укажи:\n"
            "«Недостаточно данных в предоставленных клинических рекомендациях».\n\n"
            "🔹 Инструкция:\n"
            "Сопоставь историю лечения пациента (из блока «Данные пациента») с предоставленными "
            "ниже клиническими рекомендациями из блока «Клинические рекомендации».\n"
            "Сконцентрируйся на следующем:\n"
            "— какие этапы лечения соответствуют рекомендациям;\n"
            "— какие отклоняются;\n"
            "— возможные альтернативные варианты, если они прямо предусмотрены предоставленными "
            "рекомендациями;\n"
            "— молекулярно-генетические данные (EGFR, KRAS, NRAS, BRAF, BRCA1/2 и др.), если "
            "указаны, и соответствие назначенных препаратов рекомендациям.\n\n"
            "Подавай текст:\n"
            "— без таблиц, списков, ключ-значение, Python или JSON;\n"
            "— минимум пересказа истории болезни;\n"
            "— профессиональным, корректным тоном (коллега — коллеге);\n"
            "— избегай повторов;\n"
            "— используй термин «прогрессирование»;\n"
            "— не упоминай организации или документы, если они не указаны в тексте рекомендаций.\n\n"
            "В конце ответа ОБЯЗАТЕЛЬНО добавь фразу:\n\n"
            "Не является медицинской рекомендацией! Материал создан нейросетью. "
            "Используйте в ознакомительных целях."
        ),
        "patient": (
            "Ты — врач-онколог с 10-летним опытом работы в ведущем онкологическом центре. "
            "Твой собеседник — пациент, проходящий лечение.\n\n"
            "Твоя задача — объяснить, что происходит, почему выбрана именно эта тактика, и "
            "насколько она соответствует современным официальным клиническим рекомендациям, "
            "предоставленным ниже в разделе «Клинические рекомендации», — доступным и "
            "поддерживающим языком. Пациент должен начать доверять.\n\n"
            "❗ Важно:\n"
            "— Используй ТОЛЬКО те клинические рекомендации, которые приведены ниже.\n"
            "— Не используй внешние знания и не добавляй сведения, которых нет в предоставленных "
            "фрагментах.\n"
            "— Не упоминай документы или организации, если они прямо не указаны в тексте "
            "рекомендаций.\n"
            "— Не выдумывай статистику и препараты.\n"
            "— Не фантазируй.\n"
            "— Не пересказывай многократно историю болезни, сосредоточься на объяснении и "
            "поддержке.\n\n"
            "🔹 Твои действия:\n\n"
            "1. Кратко и понятно обозначь ситуацию:\n"
            "— диагноз и основные особенности, простыми словами;\n"
            "— что уже сделано (операция, химиотерапия), когда и зачем;\n"
            "— текущая стадия и признаки прогрессирования или стабильности.\n\n"
            "2. Объясни, почему выбрана именно эта тактика, ссылаясь только на предоставленные "
            "клинические рекомендации:\n"
            "— Используй доступные формулировки, избегай уровней доказательности или классов "
            "рекомендаций;\n"
            "— Объясняй человеческим языком, фокусируясь на том, что важно знать пациенту.\n\n"
            "3. Если что-то не по стандарту — мягко, без обвинений:\n"
            "— «Это не стандартный подход, но он мог быть выбран с учётом индивидуальных "
            "особенностей».\n"
            "— Не называй решение ошибкой, если это прямо не следует из рекомендаций.\n\n"
            "4. Дай ориентир на ближайшее время (только если это прямо следует из предоставленных "
            "рекомендаций):\n"
            "— следующий этап лечения;\n"
            "— необходимость контрольных обследований;\n"
            "— наблюдение за побочными эффектами.\n\n"
            "5. Если данных не хватает — честно сообщи:\n"
            "— «Чтобы точнее оценить ситуацию, нам важно уточнить…»\n"
            "— «Без этих данных сложно определить наиболее подходящую тактику на основании "
            "клинических рекомендаций».\n\n"
            "🔹 Правила подачи:\n\n"
            "— Говори как человек, а не как протокол;\n"
            "— Избегай паники и катастрофизма;\n"
            "— Не используй латинские термины без пояснения;\n"
            "— Не упоминай «не соответствует рекомендациям» без оговорки;\n"
            "— Не добавляй препаратов или схем, если их нет в тексте рекомендаций ниже;\n"
            "— Подчёркивай командный подход и сохранение качества жизни;\n"
            "— Минимизируй повторение одних и тех же фраз.\n\n"
            "В конце ответа ОБЯЗАТЕЛЬНО добавь фразу:\n\n"
            "Не является медицинской рекомендацией! Материал создан нейросетью. "
            "Используйте в ознакомительных целях."
        ),
    }

    def __init__(self, model_name: str, device: str = "cpu") -> None:
        """
        Initialize the local generator with a HuggingFace chat model.

        Args:
            model_name: Name or path of the HuggingFace causal language model.
            device: Device identifier, e.g. ``"cpu"`` or ``"cuda"``.
        """
        self.model_name = model_name
        self.device = torch.device(device)

        self.tokenizer = AutoTokenizer.from_pretrained(model_name)
        self.model = AutoModelForCausalLM.from_pretrained(model_name).to(self.device)

        if self.tokenizer.pad_token is None:
            self.tokenizer.pad_token = self.tokenizer.eos_token

    def _get_system_prompt(self, mode: str) -> str:
        """
        Return the system prompt for the given generation mode.

        Args:
            mode: ``"doctor"`` or ``"patient"``.

        Raises:
            ValueError: If *mode* is not recognized.
        """
        if mode not in self._SYSTEM_PROMPTS:
            raise ValueError(
                f"Unknown mode '{mode}'. Must be one of: "
                f"{list(self._SYSTEM_PROMPTS.keys())}"
            )
        return self._SYSTEM_PROMPTS[mode]

    def generate(
        self,
        patient_text: str,
        retrieved_sections: list[str],
        mode: str,
        max_new_tokens: int = 512,
    ) -> str:
        """
        Generate a clinical recommendation from guideline sections and patient data.

        The prompt is formatted with ``apply_chat_template`` to ensure proper
        instruction-following behaviour for chat-tuned models (e.g. Qwen2-Instruct).

        Args:
            patient_text: Text containing patient information.
            retrieved_sections: List of relevant guideline section texts.
            mode: Generation mode; ``"doctor"`` or ``"patient"``.
            max_new_tokens: Maximum number of new tokens to generate.

        Returns:
            Generated recommendation text (newly produced tokens only).
        """
        system_prompt = self._get_system_prompt(mode)
        retrieved_context = "\n\n".join(retrieved_sections)

        user_message = (
            f"=== Клинические рекомендации ===\n{retrieved_context}\n\n"
            f"=== Данные пациента ===\n{patient_text}"
        )

        messages = [
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_message},
        ]

        # apply_chat_template adds model-specific special tokens and roles.
        text = self.tokenizer.apply_chat_template(
            messages,
            tokenize=False,
            add_generation_prompt=True,
        )

        inputs = self.tokenizer(
            text,
            return_tensors="pt",
            truncation=True,
        ).to(self.device)

        with torch.no_grad():
            output_ids = self.model.generate(
                **inputs,
                max_new_tokens=max_new_tokens,
                do_sample=False,
                pad_token_id=self.tokenizer.pad_token_id,
            )

        # Decode only the newly generated tokens.
        input_length = inputs["input_ids"].shape[1]
        new_tokens = output_ids[0][input_length:]
        generated = self.tokenizer.decode(new_tokens, skip_special_tokens=True)
        return self._ensure_disclaimer(generated)

    def _ensure_disclaimer(self, text: str) -> str:
        """
        Append the mandatory disclaimer if the model omitted it.

        The disclaimer is required by the system prompt, but may be truncated
        when max_new_tokens is reached before the model finishes generating.
        """
        if "Не является медицинской рекомендацией" not in text:
            return text + self._DISCLAIMER
        return text
